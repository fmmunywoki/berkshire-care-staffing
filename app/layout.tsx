import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Berkshire Care Staffing | Care Staffing Agency in Berkshire",

  description:
    "Healthcare staffing agency supplying healthcare assistants, support workers and care staff across Berkshire.",

  keywords: [
    "Care staffing agency in Berkshire",
    "Healthcare assistants Reading",
    "Support workers Berkshire",
    "Temporary care staff UK",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
